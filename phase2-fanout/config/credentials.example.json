{
  "_comment": "CHẾ ĐỘ CUSTOM (phương án B — 8 app). Mỗi org 1 (app_id, app_secret) → tenant_access_token thẳng, KHÔNG cần app_ticket. Copy → credentials.json (đã .gitignore) và điền secret thật. Chế độ ISV KHÔNG dùng file này (chỉ cần 1 app_id/app_secret ISV + app_ticket trong .env).",
  "byTenant": {
    "TENANT_ORG1": { "app_id": "cli_org1_xxx", "app_secret": "SECRET_ORG1" },
    "TENANT_ORG2": { "app_id": "cli_org2_xxx", "app_secret": "SECRET_ORG2" }
  }
}
